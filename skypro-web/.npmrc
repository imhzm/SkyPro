# nodemailer 9.0.5 is pinned for CVE remediation (audit fix).
# next-auth@5.0.0-beta.32 declares nodemailer as a *peerOptional* dep
# (^7 || ^8) for its email provider, which we do NOT use — we send mail
# directly via nodemailer. Skipping peer resolution is deliberate.
legacy-peer-deps=true
